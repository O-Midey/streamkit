# Installation

```bash
npm install streamkit
# or
pnpm add streamkit
```

streamkit has two runtime dependencies (`marked` for markdown lexing, `dompurify` for HTML sanitization) and peer-depends on `react >= 18`.
